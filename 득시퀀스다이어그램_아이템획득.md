```mermaid
sequenceDiagram
    autonumber
    actor Player as 플레이어
    participant UI as Add_Item_UI 
    participant Ctrl as 전투/물품컨트롤러
    participant Member as 플레이어
    participant Char as 캐릭터
    participant Inv as 인벤토리 
    participant Item as 아이템 

    Player ->> UI: 아이템 획득 요청 (플레이어Id, 아이템명, 타입, 가치)
    activate UI
    
    UI ->> Ctrl: 아이템획득(플레이어Id, 캐릭터객체, 아이템명, 타입, 가치)
    activate Ctrl
    
    %% 1. 플레이어 체크
    Ctrl ->> Member: 플레이어체크(플레이어Id)
    activate Member
    Member -->> Ctrl: 검증 결과 (true/false)
    deactivate Member
    
    alt 플레이어 검증 실패 (id != "hero")
        Ctrl -->> UI: "[획득 실패] 올바른 플레이어가 아닙니다."
    else 플레이어 검증 성공
        
        %% 2. 인벤토리 용량 확인 (캐릭터를 통해 인벤토리에 접근)
        Ctrl ->> Char: get인벤토리()
        activate Char
        Char -->> Ctrl: 인벤토리 객체
        deactivate Char
        
        Ctrl ->> Inv: get현재아이템개수()
        activate Inv
        Inv -->> Ctrl: 개수 반환
        deactivate Inv
        
        alt 인벤토리 가득 참 (개수 >= 10)
            Ctrl -->> UI: "[획득 실패] 인벤토리가 가득 찼습니다."
        else 인벤토리 여유 있음 (개수 < 10)
            
            %% 3. 가치에 따른 등급 부여 및 아이템 객체 생성 (Composition)
            note over Ctrl: 가치 판단<br/>- 1000 이상: 전설<br/>- 500 이상: 희귀<br/>- 500 미만: 일반
            
            Ctrl ->> Item: new 아이템(명, 타입, 가치, 등급)
            activate Item
            Item -->> Ctrl: 생성된 아이템 객체
            deactivate Item
            
            %% 4. 인벤토리에 아이템 추가
            Ctrl ->> Inv: 아이템추가(아이템 객체)
            activate Inv
            Inv ->> Inv: 아이템리스트에 추가
            Inv -->> Ctrl: 추가 완료
            deactivate Inv
            
            Ctrl -->> UI: "[획득 성공] 아이템명 (등급) 획득 완료!"
        end
    end
    
    deactivate Ctrl
    UI -->> Player: 결과 화면 출력 (결과 메시지)
    deactivate UI