```mermaid
sequenceDiagram
    actor 플레이어
    participant UI as :Create_Character_UI
    participant B as :전투
    participant P as :플레이어
    participant W as :전사
    participant M as :마법사

    플레이어 ->> UI: 캐릭터 생성 정보 입력(플레이어id, 캐릭터명, 직업, 레벨)
    UI ->> B: 캐릭터생성(플레이어id, 캐릭터명, 직업, 레벨)
    activate B
    
    B ->> P: 플레이어체크(플레이어id)
    activate P
    P -->> B: 검증결과(boolean)
    deactivate P

    %% 1차 검증: 플레이어 ID 체크 분기
    alt 검증결과 == true ("hero" 일 경우)
        
        %% 2차 검증: 직업에 따른 객체 생성 분기
        alt 직업 == "전사"
            B ->> W: «create» 전사(캐릭터명, 레벨)
            activate W
            W -->> B: 전사 객체 반환 (HP=레벨*100, 공격력=레벨*15)
            deactivate W
        else 직업 == "마법사"
            B ->> M: «create» 마법사(캐릭터명, 레벨)
            activate M
            M -->> B: 마법사 객체 반환 (HP=레벨*60, 공격력=레벨*25)
            deactivate M
        end
        
        B -->> UI: 생성된 캐릭터 객체 반환 (캐릭터)
        UI -->> 플레이어: 캐릭터 생성 성공 및 능력치 출력
        
    else 검증결과 == false ("hero"가 아닐 경우)
        B -->> UI: null 반환 (생성 실패)
        UI -->> 플레이어: 오류 메시지 출력 ("올바른 플레이어가 아닙니다.")
    end
    deactivate B