```mermaid
sequenceDiagram
    actor 사용자 as 플레이어
    participant UI as :Create_Character_UI
    participant B as :전투
    participant P as :플레이어
    participant W as :전사
    participant M as :마법사

    사용자 ->> UI: 캐릭터 생성 정보 입력(플레이어id, 캐릭터명, 직업, 레벨)
    
    %% 클래스 다이어그램의 메서드명과 완벽히 일치
    UI ->> B: 캐릭터생성(플레이어id, 캐릭터명, 직업, 레벨)
    activate B
    
    B ->> P: 플레이어체크(플레이어id)
    activate P
    P -->> B: 검증결과(boolean)
    deactivate P

    alt 검증결과 == true ("hero" 일 경우)
        %% 요구사항: 직업에 따른 분기 및 능력치 차등 부여
        alt 직업 == "전사"
            %% <<create>> 스테레오타입을 사용하여 전사 객체 생성 명시
            B ->> W: «create» (캐릭터명, 레벨)
            Note right of W: 전사 능력치 초기화<br/>HP = 레벨 * 100<br/>공격력 = 레벨 * 15
            W -->> B: 전사 객체 반환
        else 직업 == "마법사"
            B ->> M: «create» (캐릭터명, 레벨)
            Note right of M: 마법사 능력치 초기화<br/>HP = 레벨 * 60<br/>공격력 = 레벨 * 25
            M -->> B: 마법사 객체 반환
        end
        
        %% 클래스 다이어그램의 반환 타입(캐릭터) 일치
        B -->> UI: 캐릭터 객체 반환
        UI -->> 사용자: 캐릭터 생성 성공 및 능력치 출력
        
    else 검증결과 == false ("hero"가 아닐 경우)
        B -->> UI: null 반환 (생성 실패)
        UI -->> 사용자: 오류 메시지 출력 ("올바른 플레이어가 아닙니다.")
    end
    deactivate B