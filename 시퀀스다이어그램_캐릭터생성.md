```mermaid
sequenceDiagram
    autonumber
    actor 플레이어
    participant UI as :Create_Character_UI
    participant B as :전투
    participant P as :플레이어

    플레이어 ->> UI: 캐릭터 생성 정보 입력(플레이어id, 캐릭터명, 직업, 레벨)
    UI ->> B: 캐릭터생성(플레이어id, 캐릭터명, 직업, 레벨)
    activate B
    
    B ->> P: 플레이어체크(플레이어id)
    activate P
    P -->> B: 검증결과(boolean)
    deactivate P

    alt 검증결과 == true ("hero" 일 경우)
        
        alt 직업 == "전사"
            create participant W as :전사
            B ->> W: «create» 전사(캐릭터명, 레벨)
            activate W
            %% [핵심] 리턴이 아니라 생성된 객체 내부에서 능력치를 세팅하는 Self-Message 적용
            W ->> W: 능력치 초기화(HP=레벨*100, 공격력=레벨*15)
            %% 리턴은 깔끔하게 객체 타입만 반환
            W -->> B: 전사객체 (캐릭터 타입)
            deactivate W
        else 직업 == "마법사"
            create participant M as :마법사
            B ->> M: «create» 마법사(캐릭터명, 레벨)
            activate M
            %% [핵심] 마법사 객체의 내부 상태 초기화
            M ->> M: 능력치 초기화(HP=레벨*60, 공격력=레벨*25)
            M -->> B: 마법사객체 (캐릭터 타입)
            deactivate M
        end
        
        B -->> UI: 캐릭터객체
        UI -->> 플레이어: 캐릭터 생성 성공 및 능력치 출력
        
    else 검증결과 == false ("hero"가 아닐 경우)
        B -->> UI: null 반환
        UI -->> 플레이어: 오류 메시지 출력 ("올바른 플레이어가 아닙니다.")
    end
    deactivate B