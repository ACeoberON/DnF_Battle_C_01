```mermaid
sequenceDiagram
    actor 플레이어
    participant UI as :Add_Item_UI
    participant B as :전투
    participant P as :플레이어
    participant I as :인벤토리

    플레이어 ->> UI: 아이템 획득 정보 입력(플레이어id, 아이템명, 타입, 가치)
    UI ->> B: 아이템획득(플레이어id, 캐릭터객체, 아이템명, 아이템타입, 아이템가치)
    activate B

    B ->> P: 플레이어체크(플레이어id)
    activate P
    P -->> B: 검증결과(boolean)
    deactivate P

    alt 검증결과 == true ("hero" 일 경우)

        %% 노트(Note) 박스를 제거하고 alt 프레임을 통한 논리적 등급 판정 구현
        alt 가치 >= 1000
            B ->> B: 등급 = "전설"
        else 가치 >= 500
            B ->> B: 등급 = "희귀"
        else 가치 < 500
            B ->> B: 등급 = "일반"
        end

        %% 아이템 객체 동적 생성 (UML 표준 준수)
        create participant Item as :아이템
        B ->> Item: «create» 아이템(아이템명, 아이템타입, 아이템가치, 등급)
        
        %% 캡슐화 원칙 완벽 준수: 전투가 인벤토리 내부를 검사하지 않고 '추가'만 지시
        B ->> I: 아이템추가(아이템객체)
        activate I
        %% 인벤토리 스스로 최대용량(10) 초과 여부를 판단
        alt 인벤토리 여유 있음 (현재 개수 < 최대용량)
            I ->> I: 아이템리스트.add(아이템객체)
            I -->> B: true (추가성공)
        else 인벤토리 가득 참 (현재 개수 >= 최대용량)
            I -->> B: false (추가실패)
        end
        deactivate I

        %% 인벤토리의 반환 결과(boolean)에 따라 최종 UI 응답 결정
        alt 추가 결과 == true
            B -->> UI: 획득 성공 메시지 반환 (String)
            UI -->> 플레이어: "[아이템명] (등급) 획득 완료!" 출력
        else 추가 결과 == false
            B -->> UI: 인벤토리 포화 메시지 반환 (String)
            UI -->> 플레이어: "인벤토리가 가득 찼습니다." 출력
        end

    else 검증결과 == false ("hero"가 아닐 경우)
        B -->> UI: 인증 실패 에러 메시지 반환 (String)
        UI -->> 플레이어: "올바른 플레이어가 아닙니다." 출력
    end
    deactivate B