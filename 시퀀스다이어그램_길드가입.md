```mermaid
sequenceDiagram
    actor 플레이어
    participant UI as :Join_Guild_UI
    participant B as :전투
    participant P as :플레이어
    participant G as :길드

    플레이어 ->> UI: 길드가입 정보 입력(플레이어id, 길드명)
    %% [수정 1] CD 명세와 완벽히 일치하도록 '길드명'으로 파라미터 수정
    UI ->> B: 길드가입(플레이어id, 캐릭터객체, 길드명)
    activate B
    
    B ->> P: 플레이어체크(플레이어id)
    activate P
    P -->> B: 검증결과(boolean)
    deactivate P

    alt 검증결과 == true ("hero" 일 경우)
        
        %% [수정 2 & 3] 캡슐화 원칙 준수: '전투'가 인원을 검사하지 않고, '길드'에게 가입을 위임
        B ->> G: 캐릭터가입(캐릭터객체)
        activate G
        %% 길드 객체 내부에서 정원 초과 여부를 스스로 판단
        alt 길드 정원 미만일 경우
            G ->> G: 캐릭터리스트.add(캐릭터객체)
            G -->> B: true (가입성공)
        else 정원 초과일 경우
            G -->> B: false (가입실패)
        end
        deactivate G

        %% 길드가 돌려준 boolean 값을 기반으로 전투 로직이 UI 응답 결정
        alt 가입 결과 == true
            B -->> UI: 길드가입 성공 메시지 반환 (String)
            UI -->> 플레이어: "[길드명] 가입 완료!" 출력
        else 가입 결과 == false
            B -->> UI: 정원 초과 메시지 반환 (String)
            UI -->> 플레이어: "길드 정원이 가득 찼습니다." 출력
        end

    else 검증결과 == false ("hero"가 아닐 경우)
        B -->> UI: 인증 실패 에러 메시지 반환 (String)
        UI -->> 플레이어: "올바른 플레이어가 아닙니다." 출력
    end
    deactivate B