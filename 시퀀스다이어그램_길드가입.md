```mermaid
sequenceDiagram
    actor 플레이어
    participant UI as :Join_Guild_UI
    participant B as :전투
    participant P as :플레이어
    participant G as :길드

    플레이어 ->> UI: 길드가입 정보 입력(플레이어id, 길드명)
    UI ->> B: 길드가입(플레이어id, 캐릭터객체, 길드객체)
    activate B

    B ->> P: 플레이어체크(플레이어id)
    activate P
    P -->> B: 검증결과 (boolean)
    deactivate P

    alt 검증결과 == true ("hero" 일 경우)

        %% Aggregation: 길드는 이미 외부에서 생성된 기존 객체이므로 create 하지 않고 참조만 사용
        B ->> G: 캐릭터가입(캐릭터객체)
        activate G

        %% 길드 내부에서 정원(최대 5명) 검사
        alt 현재인원 < 최대인원 (5명 미만)
            G ->> G: 캐릭터리스트.add(캐릭터객체)
            G -->> B: 가입성공 (true)
            B -->> UI: 길드가입 성공 메시지 반환 (String)
            UI -->> 플레이어: "[길드명] 가입 완료!" 출력
        else 현재인원 >= 최대인원 (정원 초과)
            G -->> B: 가입실패 (false)
            B -->> UI: 정원 초과 메시지 반환 (String)
            UI -->> 플레이어: "길드 정원이 가득 찼습니다." 출력
        end
        deactivate G

    else 검증결과 == false ("hero"가 아닐 경우)
        B -->> UI: 인증 실패 에러 메시지 반환 (String)
        UI -->> 플레이어: "올바른 플레이어가 아닙니다." 출력
    end
    deactivate B
