```mermaid
sequenceDiagram
    autonumber
    actor 사용자
    participant UI as :Attack_Monster_UI
    participant Control as :전투
    participant Player as :플레이어
    participant Char as :캐릭터(전사/마법사)

    사용자->>+UI: 공격명령입력(플레이어id)
    
    UI->>+Control: 몬스터공격(플레이어id)
    
    %% 플레이어 체크
    Control->>+Player: 플레이어체크(플레이어id)
    Player-->>-Control: 검증결과 (boolean)
    
    %% alt 분기: 플레이어 검증 결과에 따른 처리
    alt 검증결과 == true ("hero" 일 경우)
        
        Control->>+Char: 스킬발동()
        Note right of Char: 전사: 공격력 x 1.5<br/>마법사: 공격력 x 2.0
        Char-->>-Control: 데미지 (double)
        
        Control->>+Control: 데미지등급부여(데미지)
        Note right of Control: S급: 200 이상<br/>A급: 100 이상<br/>B급: 100 미만
        Control-->>-Control: 등급 (String)
        
        %% 에러 방지: 분기문 내에서는 '-' 기호 생략
        Control-->>UI: 몬스터 공격 완료 (데미지, 등급)
        
        UI->>+UI: 결과출력(데미지, 등급)
        UI-->>-UI: 출력 완료
        
        UI-->>사용자: 최종 전투 결과 확인
        
    else 검증결과 == false ("hero"가 아닐 경우)
        
        %% 에러 방지: 분기문 내에서는 '-' 기호 생략
        Control-->>UI: 검증 실패 에러 반환
        UI-->>사용자: 접근 불가 에러 메시지
        
    end
    
    %% 분기문이 끝난 후 한 번에 비활성화 처리
    deactivate Control
    deactivate UI