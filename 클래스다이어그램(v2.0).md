```mermaid
classDiagram
    %% 1. Boundary Classes (UI 계층)
    class Create_Character_UI { <<boundary>> }
    class Attack_Monster_UI { <<boundary>> }
    class Add_Item_UI { <<boundary>> }
    class Join_Guild_UI { <<boundary>> }

    %% 2. Entity Classes (핵심 도메인 객체)
    class 플레이어 {
        +플레이어체크(플레이어id: String) boolean
    }

    class 캐릭터 {
        <<abstract>>
        +캐릭터명: String
        +레벨: int
        +HP: int
        +공격력: int
        +인벤토리객체: 인벤토리
        +«abstract» 스킬발동() double
    }

    class 전사 {
        +스킬발동() double
    }

    class 마법사 {
        +스킬발동() double
    }

    class 아이템 {
        +아이템명: String
        +타입: String
        +가치: int
        +등급: String
    }

    class 인벤토리 {
        +아이템리스트: List~아이템~
        +최대용량: int
        +아이템추가(아이템객체: 아이템) boolean
    }

    class 길드 {
        +길드명: String
        +캐릭터리스트: List~캐릭터~
        +최대인원: int
        +캐릭터가입(캐릭터객체: 캐릭터) boolean
    }

    %% 3. Control Class (비즈니스 로직 제어)
    class 전투 {
        +캐릭터생성(플레이어id: String, 캐릭터명: String, 직업: String, 레벨: int) 캐릭터
        +몬스터공격(플레이어id: String, 캐릭터객체: 캐릭터) String
        +아이템획득(플레이어id: String, 캐릭터객체: 캐릭터, 아이템명: String, 아이템타입: String, 아이템가치: int) String
        +길드가입(플레이어id: String, 캐릭터객체: 캐릭터, 길드명: String) String
    }

    %% 4. 관계 정의 (Relationships)
    %% 상속 관계 (Inheritance)
    캐릭터 <|-- 전사 : 상속
    캐릭터 <|-- 마법사 : 상속

    %% [핵심 학습 포인트] 복합 객체 관계 (Composition & Aggregation)
    %% 강한 결합 (Composition, 까만 다이아몬드 *--)
    캐릭터 *-- "1" 인벤토리 : 소유 (Composition)
    인벤토리 *-- "0..10" 아이템 : 보관 (Composition)

    %% 느슨한 결합 (Aggregation, 하얀 다이아몬드 o--)
    길드 o-- "0..5" 캐릭터 : 소속 (Aggregation)

    %% 의존 및 호출 관계 (Dependency)
    전투 ..> 플레이어 : 의존 (사용)
    전투 ..> 캐릭터 : 의존 (사용)
    전투 ..> 아이템 : 의존 (생성)
    전투 ..> 길드 : 의존 (사용)
    
    Create_Character_UI ..> 전투 : 호출
    Attack_Monster_UI ..> 전투 : 호출
    Add_Item_UI ..> 전투 : 호출
    Join_Guild_UI ..> 전투 : 호출