```mermaid
graph LR
    사용자((플레이어))
    
    %% 유스케이스는 반드시 타원형([ ])으로 선언해야 합니다.
    %% 액터와의 연결은 방향성 없는 실선(---)을 사용합니다.
    사용자 --- U1([캐릭터생성])
    사용자 --- U2([몬스터공격])
    
    %% HTML 태그 충돌을 막고 UML 표준을 지키기 위해 특수기호(« »)를 사용합니다.
    U1 -. "«include»" .-> U3([플레이어체크])
    U2 -. "«include»" .-> U3([플레이어체크])