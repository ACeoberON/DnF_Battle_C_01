```mermaid
sequenceDiagram
    actor 사용자
    participant UI as :Attack_Monster_UI
    participant B as :전투
    participant P as :플레이어
    participant C as :캐릭터(전사/마법사)

    사용자 ->> UI: 공격명령입력
    UI ->> B: 몬스터공격(플레이어id, 캐릭터객체)
    activate B
    
    B ->> P: 플레이어체크(플레이어id)
    activate P
    P -->> B: 검증결과 (boolean)
    deactivate P

    alt 검증결과 == true ("hero" 일 경우)
        B ->> C: 스킬발동()
        activate C
        C -->> B: 데미지 (double)
        deactivate C
        
        %% 요구사항의 등급 판정을 if-else 프레임(alt)으로 완벽히 표현
        alt 데미지 >= 200
            B -->> UI: S급 전투 결과 반환 (String)
            UI -->> 사용자: S급 결과 출력
        else 데미지 >= 100
            B -->> UI: A급 전투 결과 반환 (String)
            UI -->> 사용자: A급 결과 출력
        else 데미지 < 100
            B -->> UI: B급 전투 결과 반환 (String)
            UI -->> 사용자: B급 결과 출력
        end
        
    else 검증결과 == false ("hero"가 아닐 경우)
        B -->> UI: 인증 실패 에러 메시지 반환 (String)
        UI -->> 사용자: 접근 불가 에러 메시지 출력
    end
    deactivate B