```mermaid
sequenceDiagram
    actor 사용자
    participant UI as :Attack_Monster_UI
    participant B as :전투
    participant P as :플레이어
    participant C as :캐릭터(전사/마법사)

    사용자 ->> UI: 공격명령입력
    
    %% [수정 1] 클래스 다이어그램에 명시된 대로 파라미터 2개(플레이어id, 캐릭터객체)를 모두 전달
    UI ->> B: 몬스터공격(플레이어id, 캐릭터객체)
    activate B
    
    B ->> P: 플레이어체크(플레이어id)
    activate P
    P -->> B: 검증결과 (boolean)
    deactivate P

    alt 검증결과 == true ("hero" 일 경우)
        B ->> C: 스킬발동()
        activate C
        C -->> B: 데미지 (double)
        deactivate C
        
        %% 내부 로직은 불필요한 Self-message 메서드 호출 대신 Note로 처리하여 클래스 다이어그램과 일관성 유지
        Note right of B: 데미지 기반 등급 계산<br/>S급: 200 이상<br/>A급: 100 이상<br/>B급: 100 미만
        
        %% [수정 2] 반환 타입을 클래스 다이어그램과 동일하게 String 형태의 결과 문자열로 반환
        B -->> UI: 전투 결과 메시지 (String)
        UI -->> 사용자: 최종 전투 결과 출력
        
    else 검증결과 == false
        B -->> UI: 인증 실패 에러 메시지 (String)
        UI -->> 사용자: 접근 불가 에러 메시지 출력
    end
    deactivate B